<h1>id: <%= @task.id %>のタスク編集ページ</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '編集完了' %>
  
<% end %>

<%= link_to '一覧に戻る', tasks_path %>